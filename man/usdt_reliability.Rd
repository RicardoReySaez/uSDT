% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{usdt_reliability}
\alias{usdt_reliability}
\alias{summary.usdt_reliability}
\alias{print.usdt_reliability}
\title{Reliability of the direct and indirect measures}
\usage{
usdt_reliability(object)

\method{summary}{usdt_reliability}(object, ...)

\method{print}{usdt_reliability}(x, ...)
}
\arguments{
\item{object}{An \code{hsdt} object from \code{\link[=hsdt]{hsdt()}}, which may also carry the
results of \code{\link[=usdt_boot]{usdt_boot()}}. The summary method takes the
\code{usdt_reliability} object that this function returns.}

\item{...}{Ignored.}

\item{x}{A \code{usdt_reliability} object.}
}
\value{
An object of class \code{usdt_reliability}. Its \code{tasks} data frame gives
one reliability per task, with the two variances it comes from. Its
\code{subjects} data frame gives the \verb{d'}, the measurement variance and the
reliability of every subject. Bootstrap intervals appear in both when they
are available.
}
\description{
Estimates how much of the spread in \verb{d'} comes from real differences between
subjects, and how much comes from the noise of a limited number of trials. A
value close to one means that the task separates subjects well. A value
close to zero means that most of the observed spread is measurement error.
}
\details{
For subject \code{j} in task \code{t} the reported value is
\code{tau2_t / (tau2_t + v_tj)}. The first term, \code{tau2_t}, is the variance of the
sensitivity between subjects, which the model estimates. The second term,
\code{v_tj}, is the variance of the sensitivity that the trials of that subject
can support on their own.

\code{v_tj} depends on the number of trials and on the position of the subject on
the response curve. A cell contributes \code{n * dnorm(eta)^2 / (p * (1 - p))},
so two subjects with the same number of trials can differ in precision. The
calculation also discounts the information spent on estimating the
criterion. When the model has fixed a criterion to zero there is nothing to
discount, and the same formula applies.

This measure looks at each task alone and never uses the other task, so it
describes the information the data of one subject actually carry. The
estimates drawn by \code{\link[=plot.hsdt]{plot.hsdt()}} are more precise than this, because the
model there does borrow information across subjects and tasks.

The value reported for a whole task replaces \code{v_tj} by its average. For a
subject drawn at random, the variance of a single measurement is
\code{tau2_t + E(v_tj)}, so the ratio says which share of that spread is real.
Averaging the reliabilities of the individual subjects would answer a
different question. The average over subjects treats them all as equally
important, which suits a sample that represents the population of interest.

\code{v_tj} is the classical sampling variance of \verb{d'} of Gourevitch and Galanter
(1967), which \code{\link[=sdt_moments]{sdt_moments()}} reports as \code{var_gg}. It also equals the
standard error that a probit regression would give for the sensitivity of
that subject alone. The three are the same formula, evaluated at different
points. \code{var_gg} uses the observed rates of the subject, while \code{v_tj} uses
the rates that the hierarchical model predicts.

When \code{object} carries a usable bootstrap, the function recomputes the
reliability in every retained replicate, each one with its own variance and
its own subject estimates. Percentile intervals remain available at a
boundary. Normal and basic intervals work on the logit scale, so they are
missing when a reliability reaches zero or one.
}
\examples{
\donttest{
set.seed(1)
df <- usdt_simulate(n_subj = 30, n_trials = 80)
data <- usdt_data_long(
  df,
  task_col = "task",
  task_levels = c(direct = "D", indirect = "I"),
  subject_col = "subj",
  condition_col = "cond",
  condition_levels = c(signal = 1, noise = 0),
  response_col = "response",
  response_levels = c(signal = 1, noise = 0)
)
usdt_reliability(hsdt(data))
}

}
\references{
Gourevitch, V., & Galanter, E. (1967). A significance test for one parameter
isosensitivity functions. \emph{Psychometrika}.
}
\seealso{
\code{\link[=hsdt]{hsdt()}}, \code{\link[=usdt_boot]{usdt_boot()}}, \code{\link[=sdt_moments]{sdt_moments()}}
}
