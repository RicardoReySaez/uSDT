% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dichotomize.R
\name{meyen_split}
\alias{meyen_split}
\title{Meyen median-split dichotomization}
\usage{
meyen_split(x, by, signal = c("faster", "slower"), ties = c("noise", "random"))
}
\arguments{
\item{x}{Numeric vector with the continuous measure, usually response times.}

\item{by}{Grouping vector identifying the subject. The median is
computed within each group across all of that subject's trials.}

\item{signal}{Which side of the median counts as a \emph{signal} response.
\code{"faster"} (the default) suits priming and cueing tasks, where the signal
condition speeds responses up. Use \code{"slower"} for tasks in which the
signal condition slows responses down, such as interference paradigms.}

\item{ties}{How to handle trials falling exactly on the median. \code{"noise"}
(the default) assigns them the noise response, reproducing
\code{as.integer(median(rt) > rt)}. \code{"random"} assigns them at random so that
the split is as close to 50/50 as the number of trials allows.}
}
\value{
An integer vector of the same length as \code{x}, with \code{1} for signal
responses and \code{0} for noise responses. \code{NA} in \code{x} propagates.
}
\description{
Dichotomizes a continuous measure (typically response times) with a median
split computed \strong{within subject, pooling conditions}, following Meyen
et al. (2022). This places a continuous indirect measure on the same
sensitivity scale as a binary direct measure.
}
\details{
With an odd number of trials an exact 50/50 split is impossible: the median
is itself an observed value. \code{ties = "noise"} then yields a proportion of
\verb{(n - 1) / (2n)} rather than \code{0.5}, and the criterion is no longer exactly
zero. The deviation is negligible in practice (it is \verb{1 / (2n)}), but
\code{ties = "random"} removes it by rounding up or down at random, which is
unbiased across subjects.
}
\examples{
rt   <- c(320, 410, 295, 500, 380, 450)
subj <- rep(c("s1", "s2"), each = 3)
meyen_split(rt, by = subj)

}
\references{
Meyen, S., Zerweck, I. A., Amado, C., von Luxburg, U., & Franz, V. H.
(2022). Advancing research on unconscious priming: When can scientists claim
an indirect task advantage? \emph{Journal of Experimental Psychology: General}.
}
